{
  "name": "vila-market",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@react-google-maps/api": "^2.0.1",
    "cep-promise": "^4.0.3",
    "concurrently": "^3.5.0",
    "react-cep-promise": "^1.0.1",
    "react-geocode": "^0.2.2",
    "react-phone-input-2": "^2.13.9"
  },
  "scripts": {
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm start",
    "start": "concurrently --kill-others-on-fail \"npm run backend\" \"npm run frontend\""
  }
}
